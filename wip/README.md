# WIP Skills

This folder temporarily holds legacy skills not included in the active `probe next` routed workflow.

Notes:

- Normal `npx skills add <source> --skill '*'` discovery should not include nested skills here.
- `--full-depth` or explicit subpath targeting can still discover nested `SKILL.md` files.
- Keep active installable skills at repository root skill directories and in `meta.json`.
